spring:
  config:
    import:
      - application-auth.yml
      - application-persistence-local.yml
      - application-docs.yml
      - application-jwt.yml

jasypt:
  encryptor:
    bean: jasyptStringEncryptor
