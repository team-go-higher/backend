spring:
  config:
    import:
      - application-oauth2.yml
      - application-persistence-local.yml
springdoc:
  packagesToScan: gohigher
  swagger-ui:
    path: /api-docs
    groups:
      enabled: false
